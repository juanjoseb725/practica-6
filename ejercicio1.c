#include <stdio.h>

int main() {
    // Comentario por línea

    /* Comentario por bloque
       que puede ocupar varios renglones */

    // Este código compila y ejecuta, pero no muestra salida alguna
    // debido a que los comentarios no se toman en cuenta.

    /* Ya sea por línea o por bloque,
       los comentarios no son tomados en cuenta al momento
       de compilar el programa.
       Sólo sirven como documentación en el código fuente. */

    return 0;
}
